$(function(){
    $slider = $('.carousel.carousel-slider').carousel({
      fullWidth: true,
      indicators: true
  });
    $sliderInstance = M.Carousel.getInstance($slider);

 /* $('.modal').modal({
    opacity: 0
  });
  $dateContainer = $("#dateContainer");
  $datePicker = $('.datepicker');
  $datePicker.datepicker(
    {
      autoClose: false,
      container: $dateContainer
    }
  ); */

});
